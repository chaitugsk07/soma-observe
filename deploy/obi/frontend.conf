# nginx config for the frontend service.
# Every inbound request is proxied to the backend, creating a
# frontend -> backend dependency edge that OBI/Beyla will capture.

server {
    listen 8080;

    location / {
        proxy_pass http://backend/;
        proxy_set_header Host backend;
    }
}
